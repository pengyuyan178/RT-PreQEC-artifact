seed: 42
device: cpu
output_dir: results/runs
log_level: INFO

model:
  type: candidate_predecoder
  detector_feature_dim: 8
  candidate_feature_dim: 10
  hidden_dim: 64
  scorer: bilinear
  pooling: mean_max
  dropout: 0.0
